[tox]
envlist = py26, py27, py33, py34, py35, py36

[testenv]
commands = coverage run --source ascii_canvas -m pytest --cov=ascii_canvas --cov-report=term tests/ --cov-config .coveragerc
deps =
    pytest
    pytest-cov
